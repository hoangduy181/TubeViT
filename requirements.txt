click~=8.1.7
torch~=2.0.1
torchvision~=0.15.2
lightning~=2.0.7
torchmetrics~=1.0.3
pytorchvideo==0.1.5
matplotlib~=3.7.2
seaborn~=0.12.2
tensorboard~=2.15.0
protobuf<5.0.0,>=3.20.0  # Pin protobuf version for TensorBoard compatibility
av  # PyAV - required for torchvision video operations
pyyaml~=6.0.1  # For configuration file support

# Optional: For FLOPS calculation (install one of these)
# fvcore>=0.1.5  # Facebook's fvcore for FLOPS calculation
thop>=0.1.1  # Alternative FLOPS calculation library